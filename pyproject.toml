[project]
name = "arithmetic-encoder"
version = "0.1.0"
description = "Adaptive arithmetic coding experiment for lowercase text."
readme = "README.md"
requires-python = ">=3.9"

[tool.pytest.ini_options]
python_files = ["test_*.py"]